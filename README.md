# lightly
The fastest Cloud Network (CN) in Canada
# Setup
## JavaScripts:
* ~~~`dist/lightly.min.js`~~~
* `dist/js-manifest`
## YAML's:
* `yaml/invokes.yml`
* `yaml/yaml-manifest`
## PHP's:
* `dist/lightl.php`
* * *
So, in `dist/lightly.php`, type:
```php
fcreate("lightly.zip");
```
Then:
```
fopen("lightly.zip");
```
